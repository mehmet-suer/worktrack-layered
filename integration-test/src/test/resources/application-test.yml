spring:
  main:
    allow-bean-definition-overriding: true
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
      enabled: true


file:
  upload-dir: ${java.io.tmpdir}/worktrack-test-uploads


testcontainers:
  mysql:
    image: mysql:8.4
    database: worktrack_test
    username: test
    password: test